p.color-text-medium
    span
        | Color 
    span.text-bold
        | {{colors[colorId].name}}
    .color-circles-wrapper
        .color-circle(ng-repeat='color in colors track by $index' ng-style='{"background-color":color.hex}' ng-click='chooseColor($index)')
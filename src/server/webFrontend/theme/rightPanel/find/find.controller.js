
module.exports = function ($scope, productsGetter) {
    $scope.subtotal = null
    this.$onInit = () => {}

    /**
     * API for passing what user types to productsGetter to retrieve from server, and list it as serie of items
     */
}